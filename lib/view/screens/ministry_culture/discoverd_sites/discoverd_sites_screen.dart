import 'package:flutter/material.dart';

class DiscoverSiteScreen extends StatelessWidget {
  const DiscoverSiteScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
